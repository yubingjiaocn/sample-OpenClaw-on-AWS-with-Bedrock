# Python
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
admin-console/dist/
admin-console/node_modules/

# IDE
.vscode/
.DS_Store
**/.DS_Store

# Build artifacts
*.pkg
enterprise/admin-console/dist/

# Terraform
eks/terraform/.terraform/
eks/terraform/*.tfstate
eks/terraform/*.tfstate.*
eks/terraform/*.tfvars
!eks/terraform/*.tfvars.example
# .terraform.lock.hcl is intentionally committed: it pins provider versions so
# CodeBuild/event provisioning resolves the exact same providers every run
# (open-ended constraints previously let alekc/kubectl drift and break apply).
# eks/terraform/.terraform.lock.hcl

# Demo
enterprise/demo/dist/

# Blog (local only, not part of repo)
enterprise/blog/

# Brand assets (local only)
enterprise/catnip-mascot/
enterprise/.env
enterprise/.env.*
!enterprise/.env.example
.claude/
enterprise/docs/environments.md
